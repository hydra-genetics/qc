resources: "resources.yaml"
samples: "samples.tsv"
units: "units.tsv"

default_container: "docker://hydragenetics/common:3.3.0"

reference:
  fasta: ""
  design_intervals: ""
  design_bed: ""
  wgs_intervals: ""

fastqc:
  container: "docker://hydragenetics/fastqc:0.11.9"

fgbio_collect_duplex_seq_metrics:
  container: "docker://hydragenetics/fgbio:2.1.0"

gatk_calculate_contamination:
  container: "docker://hydragenetics/gatk4:4.1.9.0"

gatk_get_pileup_summaries:
  container: "docker://hydragenetics/gatk4:4.1.9.0"
  sites: "reference/chr1_small_exac_common_3.hg19.vcf"
  variants: "reference/chr1_small_exac_common_3.hg19.vcf"

mosdepth:
  by: ""
  container: "docker://hydragenetics/mosdepth:0.3.2"
  extra: ""

mosdepth_bed:
  container: "docker://hydragenetics/mosdepth:0.3.2"
  extra: ""

multiqc:
  container: "docker://hydragenetics/multiqc:1.11"
  reports:
    DNA:
      included_unit_types: ['N', 'T']
      qc_files:
        - "qc/fastqc/{sample}_{type}_{flowcell}_{lane}_{barcode}_{read}_fastqc.zip"
        - "qc/picard_collect_alignment_summary_metrics/{sample}_{type}.alignment_summary_metrics.txt"
        - "qc/picard_collect_duplication_metrics/{sample}_{type}.duplication_metrics.txt"
        - "qc/picard_collect_gc_bias_metrics/{sample}_{type}.gc_bias.summary_metrics"
        - "qc/picard_collect_hs_metrics/{sample}_{type}.HsMetrics.txt"
        - "qc/picard_collect_insert_size_metrics/{sample}_{type}.insert_size_metrics.txt"
        - "qc/picard_collect_multiple_metrics/{sample}_{type}.{ext}"
        - "qc/picard_collect_wgs_metrics/{sample}_{type}.txt"
        - "qc/mosdepth/{sample}_{type}.mosdepth.summary.txt"
        - "qc/mosdepth_bed/{sample}_{type}.per-base.bed.gz"
        - "qc/samtools_stats/{sample}_{type}.samtools-stats.txt"
        - "prealignment/fastp_pe/{sample}_{type}_{flowcell}_{lane}_{barcode}_fastp.json"
        # Choose one somalier module based on your use case:
        - "qc/somalier_matched/somalier_samples_mqc.tsv"  # For T/N paired samples
        # - "qc/somalier_trio/somalier_samples_mqc.tsv"     # For trio/family samples
        # - "qc/somalier_ungrouped/somalier_samples_mqc.tsv"  # For ungrouped/RNA relatedness

peddy:
  container: "docker://hydragenetics/peddy:0.4.8"
  extra: ""

picard_collect_alignment_summary_metrics:
  container: "docker://hydragenetics/picard:2.25.0"

picard_collect_duplication_metrics:
  container: "docker://hydragenetics/picard:2.25.0"

picard_collect_gc_bias_metrics:
  container: "docker://hydragenetics/picard:2.25.0"

picard_collect_hs_metrics:
  container: "docker://hydragenetics/picard:2.25.0"

picard_collect_insert_size_metrics:
  container: "docker://hydragenetics/picard:2.25.0"

picard_collect_multiple_metrics:
  container: "docker://hydragenetics/picard:2.25.0"
  output_ext:
    - "gc_bias.pdf"

picard_collect_wgs_metrics:
  container: "docker://hydragenetics/picard:2.25.0"

somalier_matched_mqc:
  mqc_config: "config/somalier_mqc_config.yaml"

somalier_matched_extract:
  container: "docker://hydragenetics/somalier:0.2.18"
  sites: "reference/sites.hg19.vcf.gz"
  extra: ""

somalier_matched_relate:
  container: "docker://hydragenetics/somalier:0.2.18"
  extra: "--infer"

## Uncomment the following section to use somalier_trio module instead of somalier_matched
## Note: Only one somalier module (matched OR trio) should be configured per pipeline run

# somalier_trio_mqc:
#   mqc_config: "config/somalier_mqc_config.yaml"

# somalier_trio_extract:
#   container: "docker://hydragenetics/somalier:0.2.18"
#   sites: "reference/sites.hg19.vcf.gz"
#   extra: ""

# somalier_trio_relate:
#   container: "docker://hydragenetics/somalier:0.2.18"
#   extra: "--infer"

# somalier_trio_validate:
#   threshold: 0.4

## Uncomment the following section to use somalier_ungrouped module
## Note: Only one somalier module should be configured per pipeline run
## This module is for general relatedness checking without predefined pairs/trios

# somalier_ungrouped_mqc:
#   mqc_config: "config/somalier_mqc_config.yaml"
#
# somalier_ungrouped_extract:
#   container: "docker://hydragenetics/somalier:0.2.18"
#   sites: "reference/sites.hg19.vcf.gz"
#   extra: ""
#
# somalier_ungrouped_relate:
#   container: "docker://hydragenetics/somalier:0.2.18"
#   extra: "--infer"

verifybamid2:
  container: "docker://hydragenetics/verifybamid2:2.0.1"
  svd_mu: "verifybamid2/1000g.phase3.10k.b38.exome.vcf.gz.dat.mu"